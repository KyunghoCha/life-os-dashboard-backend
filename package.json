{
  "name": "life-os-dashboard-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Local API backend for the Life OS Dashboard prototype.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "backup": "node scripts/backup.js",
    "restore": "node scripts/restore.js",
    "reset": "node scripts/reset.js",
    "smoke": "node scripts/smoke.js"
  },
  "engines": {
    "node": ">=24 <25"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
